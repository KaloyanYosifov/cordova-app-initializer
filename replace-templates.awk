{
    gsub(/%APP_NAME%/, APP_NAME);
    gsub(/%APP_DISPLAY_NAME%/, APP_DISPLAY_NAME);
    gsub(/%APP_DESCRIPTION%/, APP_DESCRIPTION);
    gsub(/%APP_AUTHOR%/, APP_AUTHOR);
    gsub(/%APP_URL%/, APP_URL);
    print
}
